<!-- Licensed to Kethily Daniel & NDXCode under one or more agreements. -->
<!-- Kethily Daniel & NDXCode licenses this file to you under the Business Source License 1.1. -->
<!-- See the LICENSE file in the project root for more information. -->

<!--
    Copyright (c) 2026 Kethily Daniel & NDXCode. All rights reserved.

    Use of this software is governed by the Business Source License included
    in the LICENSE file and at www.mariadb.com/bsl11.

    Change Date: Four years from the date each version of the Licensed Work
    is first publicly distributed.

    On the Change Date, in accordance with the Business Source License,
    use of this software will be governed by the GNU General Public License v3.0
    or later (GPL-3.0-or-later).

    Contact Information: https://kirintool.cfd
-->
    
    <ui:ContentDialog x:Class="Kirin_Tool.UI.PartitionSelectorDialog"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  mc:Ignorable="d"
                  Title="Select Partitions to Flash"
                  DialogWidth="800"
                  DialogHeight="750"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center">

    <Border Background="White" CornerRadius="8" BorderBrush="LightGray" BorderThickness="1">
        <Grid Margin="32,24,32,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <CheckBox Grid.Column="0" 
                         x:Name="SelectAllCheckBox"
                         IsChecked="{Binding SelectAllState, Mode=TwoWay}"
                         VerticalAlignment="Center"
                             Margin="0,0,8,0"
                             MinHeight="24"
                             MinWidth="24"/>

                <TextBlock Grid.Column="1" 
                          Text="Partition Name" 
                          FontWeight="SemiBold"
                          FontSize="14"
                          VerticalAlignment="Center"/>

                <TextBlock Grid.Column="2" 
                          Text="Size" 
                          FontWeight="SemiBold"
                          FontSize="14"
                          VerticalAlignment="Center"
                          Margin="20,0,0,0"/>
            </Grid>

            <ScrollViewer Grid.Row="1" 
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         MaxHeight="500">
                <ItemsControl ItemsSource="{Binding Partitions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,4" 
                                   Padding="12,8"
                                   Background="Transparent"
                                   BorderBrush="LightGray"
                                   BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="300"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0" 
                                             IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                             VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="1" 
                                              Text="{Binding Name}"
                                              VerticalAlignment="Center"
                                              FontFamily="Consolas"
                                              FontSize="13"/>

                                    <TextBlock Grid.Column="2" 
                                              Text="{Binding FormattedSize}"
                                              VerticalAlignment="Center"
                                              Foreground="Gray"
                                              FontSize="12"
                                              Margin="20,0,0,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <TextBlock Grid.Row="2"
                      Text="{Binding StatusText}"
                      Margin="0,20,0,0"
                      Foreground="DimGray"
                      FontSize="14"
                      HorizontalAlignment="Center"/>

            <StackPanel Grid.Row="3" 
                       Orientation="Horizontal" 
                       HorizontalAlignment="Right" 
                       Margin="0,20,0,0">
                <ui:Button x:Name="CancelButton"
                          Content="Cancel"
                          Margin="0,0,10,0"
                          MinWidth="100"
                          Click="CancelButton_Click"/>
                <ui:Button x:Name="FlashButton"
                          Content="Flash Selected"
                          IsEnabled="{Binding HasSelectedPartitions}"
                          MinWidth="120"
                          Appearance="Primary"
                          Click="FlashButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</ui:ContentDialog>
